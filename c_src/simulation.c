#include "boxes.h"
#include "trial.h"

#define TRIALS 2


/*
 - initialize all random numbers needed for a trial in large array
 - copy NUM_BOXES into the static array
 - 




 Running the simulation
 - 

*/


int main() {

    // BOXES_ARRAY;

    printf("%d\n", MAX_ATTEMPTS);

    // __uint8_t* rnums = random_array(NUM_BOXES);

    // int result = run_trial(rnums);

    // printf("Result = %d\n", result);

    

   
}

